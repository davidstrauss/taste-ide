---
author: primary
created: 2026-09-16T02:07:12Z
---

Coordinator review: merged into `main` as `c6fe6fd` on 2026-09-16. Passes: two upstreams chosen per request from the placeholder's environment, credential bound to the route in one `match`, no fallback to the API when the private file is missing, quota not harvested on the private route while spend still lands per environment, and revoke taking the route with it. The `private` picker row is the IDE's and is never sent to the agent, with the `ANTHROPIC_CUSTOM_MODEL_OPTION` alternative argued and rejected in the module header and ENVIRONMENTS.md. The header replaces the Plan gauge with "Private" and the Utilization tab accounts for itself. Tests: two placeholders to two upstreams with the right key on each, the no-fallback refusal, revoke forgetting the route, the `issue_start` path in `private_route.rs`, and the picker and context-window rules in `chat.rs`. Probes at both rungs per the agent, near-miss clean. One conflict, README.md: the agent edited the long section that had since been cut to steps, so the coordinator resolved it as steps 6 and 7 (write the private-model file; pick the row and prompt), pointing the two open observations at i-0034. Gate on the merged tree: fmt clean, clippy `-D warnings` clean, every crate green except the `watcher` and `configwatch` modules, which fail on the inotify exhaustion i-0030 is fixing. Note for the record: this environment ran on the agent's default model, not on Fable as asked; `fable` is not an advertised id (i-0029), so the work was reviewed on its merits.
