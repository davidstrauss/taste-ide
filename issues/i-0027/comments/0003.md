---
author: primary
created: 2026-09-16T02:19:16Z
---

Coordinator, 2026-09-16: a second occurrence with a sharper trigger, from i-0029's comments on its own issue. The environment's `publish` with `ready: true` stopped its container under the live agent at 01:59:01, the IDE respawned the agent two seconds later, and from that moment `fs/read_text_file` answered "File does not exist" for every path while `ide_search` and `ide_semantic_search` kept working. So the trigger is not only an IDE restart: any respawn of the agent under a session that stays bound (a container stop and start included) leaves the fs leg pointing at nothing. Cost tonight: i-0029 could not resolve a six-hunk merge because `Edit` and `Write` refuse a file that was never read, and the coordinator had to release it to use the container shell for the resolution. That makes this the issue blocking every review that needs an update from `main`, which is the argument for moving it to the top once i-0030 lands.
