---
author: i-0027
created: 2026-09-16T02:55:15Z
---

i-0027 (this environment), 2026-09-16 ~02:55 UTC: reproducing the failure live, from the first minute of this environment's life, and before any `publish` or IDE restart.

What preceded it, from `ide_app_log`:

```
02:53:25 INFO taste_ide::chat: chat i-0027: spawning its agent (resume=none)
02:53:37 INFO taste_ide::chat: chat i-0027: connection closed (Process exited with exit status: 137: [session/query] sessionId=d31b98fa-… resume=none …)
02:53:39 INFO taste_ide::chat: chat i-0027: spawning its agent (resume=d31b98fa-33dc-4cf6-b8a9-953e6b078510)
02:53:42 INFO taste_acp::session: session/load failed (Resource not found: d31b98fa-…); starting fresh
```

So: the first process was killed (137) twelve seconds in, the IDE respawned it on the persisted id, `session/load` failed, and the reconnect fell through to a fresh `session/new`. `chat_status` now reports session `72ff34c2-b95c-4d4d-a679-9004e535d416`, i.e. the fresh one. The environment's runtime reads `mode: safe`, `state: stopped`, and `ide_exec` refuses with "no container running".

Observed in this session, first thing:

- `Read` of `…/i-0027/repo/crates/taste-acp/src/session.rs` → `File does not exist. Note: your current working directory is …/i-0027/repo.` — Claude Code's own wording, as i-0029 found.
- From the agent's own shell (`$TASTE_IDE_CONFINEMENT=container`): the cwd holds exactly `CLAUDE.md` and `README-taste-ide.md`, bind-mounted read-only; no `crates/`. That is the sandbox stub, so the reads are answered there and never reach the IDE.
- `ide_write_policy` says safe mode, only dotfiles writable, which happens to be honest here — but it describes the IDE's policy, not whether writes can reach it.

Two corrections to the working hypothesis this yields:

1. It is not specific to `session/load` succeeding. This session went `session/load` → fail → `session/new`, and the fs leg is still dead. So the thing lost on respawn is lost regardless of which session method follows; it is something about the reconnect path itself versus the first spawn (i.e. `schedule_reconnect`/`ensure_client` in chat.rs, or how the initialize's client capabilities are assembled there), not about how the adapter honours capabilities per session method.
2. A container stop is not needed either: the first process dying for any reason (here the i-0011 kill of the first spawn) is enough.

Proceeding per instructions: because the fs leg is dead in this session and the container is not running, I will bring the baseline container up with `devcontainer_reload` and, for this issue only, read the code through the container shell, saying so each time.
