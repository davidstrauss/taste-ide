---
author: primary
created: 2026-09-13T09:20:34Z
---

Reviewed and merged to main as a fast-forward, 68e1af0..2728d42.

Verified here rather than taken on the report's word: `cargo fmt --all -- --check` clean, `cargo clippy --workspace --all-targets -- -D warnings` clean, `cargo test --workspace` exit 0, and `build-aux/headless/typing.sh` exit 0 on the merged tree — the default burst, which is the one that used to fail. I did not independently reproduce the *before* half (exit 134); that would need the pre-fix binary built out of tree, which is i-0021.

The fix is the right one and the reasoning is sound: `matching` trimmed an optional leading slash, so interactive completion — which asks about every word — pulled the command list up over ordinary prose whenever a word prefixed a command name, and the upstream `notify::empty` race then presented a popup before its rows existed. `command_prefix` applies `is_trigger`'s stated rule to every keystroke after the first, which is where it was missing, and the earlier guard (e8c7945) could never have covered it because it only caught words matching nothing. Both of the issue's candidates were checked and dismissed with evidence rather than assumed.

The second commit is not scope creep: `is_trigger` asked for `iter.offset() == 0` while the framework calls it with the caret one past the typed character, so the trigger had never once fired, and closing the ordinary-word path without it would have left the slash opening nothing at all.

Noted, not blocking: the display-needing test in `command_completion.rs` is `#[ignore]`d and so is outside `cargo test --workspace`, with `typing.sh` as the runnable gate and CLAUDE.md saying so. That matches the `perf_` harness precedent in this repo, but it does mean CI will not catch a regression here — worth revisiting if this area moves again.
