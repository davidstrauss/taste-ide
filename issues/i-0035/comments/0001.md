---
author: primary
created: 2026-09-16T02:09:25Z
---

Triage (coordinator, 2026-09-16): kept, correctly split from i-0030, and placed under i-0034. Not started, for two reasons. First, the body's own last paragraph is the reason: "something happened to the machine" has no representation in the coordinator's transcript, and neither does an environment failing to build, blocking on a permission card, or flagging itself. That is one design, drafted on 2026-09-14 under the working title "Fleet news belongs in the orchestrator transcript, and blocking news belongs answerable there" and not yet filed, because David asked to shape it before an agent takes it. The four decisions posed to him then still stand: which moments get a card; one card per environment updated in place versus one per event; whether a blocking card is answerable in the coordinator's pane in the same issue or a follow-on; and whether a card also wakes the coordinator's agent. This issue is that design's first host-level case and should be worked as part of it or immediately after it, not before. Second, the six `Moment` variants in `notify.rs` and the review-flag prompt the window already sends the coordinator are the existing halves; the inotify condition would be a seventh moment there, raised once and cleared on recovery, which is the "not per environment, not per retry" the body asks for. i-0030's own measurement (`free: 0` for twenty minutes with four environments live) is the evidence to keep.
