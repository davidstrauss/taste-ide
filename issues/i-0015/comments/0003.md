---
author: primary
created: 2026-09-10T06:32:59Z
---

Finished at David's direction: the prompt text stays (f2eedcc) and the test now pins it (0b4e335). Both commits are on main, unpushed.

The test is `server::tests::the_instructions_carry_the_backlog_rule_and_the_coordinators_brief`. On both sockets it now asserts the reading rule — "NEVER THROUGH THE SHELL", the shell tools it names, and both reasons ("supervises", "only what is on disk") — because that rule is every agent's, not the coordinator's. On the primary's socket it additionally pins one phrase from rule 7 ("never a cat or a grep in a shell") and one from rule 9 ("implementation is an agent's work on the backlog"), neither of which follows from the tool names it already checked. The doc comment on `coordinator_brief()` that describes what the test looks for was updated to match, since being out of date is the only way that comment can hurt.

Gate: fmt clean, clippy clean with -D warnings, `cargo test --workspace` exit 0; the pinning test runs and passes.

Not done, and deliberately: `Errand::prompt` in `taste-app/src/coordinator.rs` — the words a woken coordinator gets for a review — tells it to read the branch with `git log` and `git diff`. That is git, which the new rule leaves to ide_exec, so it is correct as written. Worth a second look only if it ever grows a file read.
