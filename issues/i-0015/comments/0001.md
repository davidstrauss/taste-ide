---
author: primary
created: 2026-09-10T06:27:09Z
---

Done directly on main as f2eedcc, at the user's explicit instruction ("No, I want you to update the prompts built into this IDE to say that") rather than in an environment — which is the one waiver the new rule 9 names.

Both gaps closed, in both prompts. The MCP instructions (`server.rs:175`, what every agent in the fleet gets) now state reading-through-the-IDE as a rule rather than listing the calls as a menu, with both reasons — supervision, and unsaved buffers a shell cannot see — and reserve ide_exec for running things. The coordinator's brief (`orchestration.rs`) carries it in rule 7, where ide_exec had been the only tool named, since that text also ships as the chat preamble for adapters that do not surface MCP instructions. Rule 9 now says implementation is an agent's work on the backlog outright, instead of leaving it to be inferred from a sentence about editing a checkout.

Gate: fmt clean, clippy clean with -D warnings, `cargo test --workspace` exit 0 with no SIGSEGV. The taste-mcp test that pins the instructions still passes untouched — it keys on `issue_reorder`, `issue_start`, `review_list`, and the header line, none of which moved. It was NOT extended to pin the new rules, which the issue asked for; that is the one part left undone, and it is why these two rules could go missing in the first place.
