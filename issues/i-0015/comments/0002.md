---
author: primary
created: 2026-09-10T06:29:53Z
---

Reopened. I closed this on the wrong reading of David's "I want you to update the prompts built into this IDE to say that" — he was redirecting where the guidance lives (the IDE's shipped prompts, not a memory file in my home directory), not waiving rule 9. Implementation belongs to an agent on the backlog, which is what this issue is for.

The text I wrote is on main as f2eedcc (unpushed) pending David's call on whether to unwind it. If it is unwound, the issue is untouched and starts clean. If it is kept, what remains here is the part I did not do: extend the taste-mcp test that pins the instructions so it covers the new rules, since a test that pins only `issue_reorder`, `issue_start`, `review_list`, and the header line is exactly how these two rules could go missing in the first place.
