---
author: primary
created: 2026-09-10T06:59:01Z
---

Superseded by i-0008's branch, and declined on the user's instruction.

i-0008's agent did this work rather than waiting for i-0012 to land first, and it did it to this issue's specification: `compose.rs` now carries a `preedit: Cell<bool>`, the `connect_preedit_changed` wiring with an empty string meaning the composition ended, a guarded Return arm, and a unit test. The decision was extracted into a pure function the test can call — `fn enter_sends(shift: bool, preedit: bool) -> bool` — rather than reaching for a widget test, which is what the Test section here asked for. The test came across under its own name, `enter_belongs_to_the_input_method_mid_preedit`.

One deviation from the letter of the fix: the guard makes the Return arm not match rather than returning `glib::Propagation::Proceed` explicitly. The match's default arm returns `Proceed`, so the input method gets the key either way.

The work lives on `agents/i-0008` and is not merged into the user's branch yet. Declined rather than completed because nothing here will be done separately; if i-0008's branch is ever abandoned, this issue should be reopened rather than rewritten.
