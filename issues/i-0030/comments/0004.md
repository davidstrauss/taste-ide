---
author: i-0030
created: 2026-09-16T02:28:38Z
---

Answering the 02:03:15 ask, which arrived again as a chat message just now.

**The docs update was already in flight and is done.** `docs/ENVIRONMENTS.md`, right after the three-ceiling paragraph (`MAX_ORCHESTRATED_ENVIRONMENTS`, `MAX_ORCHESTRATED_DISK_BYTES`, `MIN_FREE_DISK_BYTES`), now has a paragraph naming `fs.inotify.max_user_instances` beside them — what it bounds, that nothing in the IDE enforces or raises it, the sysctl, and `= 512` as the number a fleet of six needs, plus this machine's measured numbers (67 gone before the IDE started, the remaining 61 taken by four environments, `free: 0` after). Committed as `bf06fc82e526f9eaa613cf17e02e217579af2a54` on `agents/i-0030`, published as a checkpoint.

**The IDE's own instance count was not reduced, so there is no before/after to give.** This fix is test-only (`InotifyLease` in `crates/taste-core/src/watcher.rs`, used by the `taste-core` and `taste-devcontainer` test suites) — it changes nothing about how many instances the running IDE opens. The one-instance-for-the-whole-fleet design for config watching (`configwatch::ConfigWatch`, `docs/ENVIRONMENTS.md` line ~251) predates this issue; I didn't touch it, so it isn't a change to measure.

**The `--test-threads=1` datapoint matches what's already shipped.** It's the same conclusion the 02:03:15 comment drew and what my 02:18:11 comment already accounted for: sharing an instance within one test binary can't be the whole answer since the pool can be empty before the binary starts, so `InotifyLease` does both — queues within a binary, and skips with a message when the host has nothing left, which is the floor.

**Still owed:** the second reading (real assertions passing on a host with room). Sampled continuously since 02:05; as of 02:27:53 the host is still at `free: 0` — 22+ minutes with nothing available to anyone. `/tmp/await-room.sh` is still armed in this environment and will run both suites for real the moment two instances free up.
