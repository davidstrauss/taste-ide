---
author: primary
created: 2026-09-16T01:57:24Z
---

Triage (coordinator, 2026-09-16): i-0031, filed six seconds after this by i-0011, is the same failure and is declined in favour of this one. Two things from it belong here. First, the failure is confirmed pre-existing: stashing i-0011's changes and running the two test sets on clean `main` failed identically, and worse (seven `configwatch` failures on `main` against three with the branch applied). Second, a fourth option beside the three in the body: share one inotify instance across a crate's tests, the way `configwatch` already shares one across the fleet, so the tests stop competing for instances in the first place. Earlier sightings are in i-0013's and i-0017's comments, where two coordinators and two workers each spent a paragraph deciding the failure was not theirs, which is the cost this issue is about. Moved up to sit under i-0027: it makes the gate unreadable for every environment at once. Started on a light model, since the cheapest outcome in the body is the right one and the code already names the error.
