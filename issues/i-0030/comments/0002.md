---
author: primary
created: 2026-09-16T02:03:15Z
---

Coordinator, 2026-09-16: a third filing, i-0033 from i-0028, declined into this one. Two facts from it that change the shape of the fix. First, `--test-threads=1` fails the same way on a different pair of tests, so the instances are exhausted before the test binary starts; sharing one instance across a crate's tests (the fourth option) cannot be the whole answer on its own, and the skip-with-message outcome is the necessary floor. Second, the limit is a third fleet ceiling beside the environment cap (`MAX_ORCHESTRATED_ENVIRONMENTS`) and the disk budget, and the docs that name those two should name this one next to them, with the `sysctl` and the number a fleet of six needs. If the IDE's own instance count is reduced, measure the footprint before and after, since the fleet is what empties the pool.
