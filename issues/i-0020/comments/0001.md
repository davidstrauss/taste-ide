---
author: primary
created: 2026-09-12T10:26:41Z
---

Reviewed and merged by the coordinator: `agents/i-0020` fast-forwarded onto main at `68e1af0` (4 commits, 23 files, +351/−181).

Both judgement calls the issue named were answered in the code rather than left to the reader. The mark is the word "Plan", chosen over a glyph with the rejected candidates named (`taste-utilization-symbolic` sits at the other end of the same row on the tab that explains both meters; `taste-human-symbolic` came out a hundred pixels from the coordinator's two-person mark), and the word sits BETWEEN the bars so it separates as well as names. Per-pane repetition is decided, not inherited: the chat column has no tab strip, so exactly one conversation is on screen and the number appears exactly once however many panes exist — the cost being one `GtkLevelBar` per conversation off the snapshot the column already fans out.

Two things beyond the ask, both good: the 1 Hz backlog tick that aged a string nobody was hovering over is gone, replaced by a `query-tooltip` computed for the reader who is actually there plus one armed wakeup at `STALE_AFTER`; and gadget mode is explicitly left WITHOUT a second gauge, with the reason written down.

Gate on the merged tree, all green: `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` (0 failed). Looked at, per the house rule: `orchestrator`+`busy`, `utilization`, and `backlog` posed at 1440x900 under Xvfb against a freshly built binary. The pair renders as intended — identity, bare context bar, wider gap, "Plan", subscription bar — the backlog header is clean and its count has room for "4 · 3 active" again, every pane reports `fit … ok`, and `TASTE_MEASURE_MIN=1 TASTE_PROBE_CHAT=busy` says `min-width chat: min=320 nat=420`, unchanged, so the second gauge did not raise the column's floor. `near-miss.py` flags only edges in the settings tab and the pinned prompt card — label text widths and card padding, none of them a widget this change touched. The GTK warnings in the probe logs are the headless container's missing dbus and GPU, not the UI.
