---
author: primary
created: 2026-09-08T16:50:06Z
---

Reviewed and merged as `5386e42` (pre-merge main was `d5bb625`). `agents/i-0007` → `ca99045`, "issue_reorder now reads issue, matching the schema it advertises".

**The change, and it took the recommended direction.** `server.rs:2235` no longer calls `issue_id_arg`; it reads `args["issue"]` with its own trim-and-nonempty check and an error that names the argument the schema actually offers — "issue_reorder needs an `issue`: an id from issue_list". The schema is untouched, so no client written against the published surface breaks, and `issue_reorder` now agrees with `issue_start`, `issue_status`, and `issue_attachment`, which is the company it keeps.

**The helper's other callers are unharmed.** `issue_id_arg` survives at `server.rs:2252` (`issue_update`) and `server.rs:2295` (`issue_link`), and both of those declare `id` in their own schemas, so they were always consistent and stay that way.

**The test is the real deliverable and it is the right test.** `issue_reorder_moves_the_issue_the_schema_names` creates two issues through the MCP surface, reorders the second to position 0 with `{"issue": …, "position": 0}` — the only payload the schema permits — and asserts the returned order is `[second, first]`. That is the call no existing test made: the checks at `server.rs:4823` and `crates/taste-acp/tests/orchestrator.rs:318` only assert that a non-orchestrator is refused, so they could never catch a name mismatch. This class of bug cannot return silently now.

**Gate, re-run here on the merge result.** `cargo fmt --all -- --check` exit 0. `cargo clippy --workspace --all-targets -- -D warnings` exit 0. `cargo test -p taste-mcp` exit 0 — 52 passed, 0 failed, with `issue_reorder_moves_the_issue_the_schema_names` among them. `cargo test --workspace --exclude taste-app` exit 0, 31 binaries clean. `taste-app` is excluded because of the SIGSEGV in its `chat::` GTK tests recorded on i-0004 — it is unrelated to this crate and predates this change.

**One correction to the commit message, for the record.** It says "Only issue_update (which does declare id) uses that helper now." Two call sites remain, `issue_update` and `issue_link`. The code is right; the count in the prose is not. Not worth amending a published branch for, but it should not go into the history uncorrected.

**Not fixed here, by design.** The issue asked whether the same schema/handler round-trip gap exists in the other orchestration tools and said to report rather than widen. The change does not say either way, so that question is still open and unexamined — worth a look by whoever is next in `server.rs`.

**Note for whoever wants to use it.** The running IDE was built before this commit, so `issue_reorder` will keep failing in this session until the app is rebuilt and restarted. That is the user's call, not an agent's.
