---
author: primary
created: 2026-09-16T01:58:45Z
---

Triage (coordinator, 2026-09-16): kept as its own issue, correctly split from i-0011, which is taking the "hold the prompt" half and leaving the readiness half here. Moved up to sit under i-0030: every start on this host pays the sixteen seconds and the two log lines it describes. Not started yet, deliberately. It changes what `has_exec_target()` and "available" mean, and `container_gate` reads both; i-0011 is editing `container_gate`'s neighbourhood right now and is not merged, so a second agent in that code before i-0011 lands would be a conflict, not a head start. Start it once i-0011 is merged, against that tree. One design note for whoever takes it: i-0011's body already stated this outcome as the *and/or* alternative ("available is not announced until an exec would succeed"), and the trap it names is real — the rung with no podman must not wait forever on a readiness check that can never pass.
