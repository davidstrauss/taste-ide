---
author: primary
created: 2026-09-16T02:03:48Z
---

Triage (coordinator, 2026-09-16): kept, correctly split from i-0028, which is flagged for review and not yet merged. Placed under i-0032 because it is the tail of the private-model thread David is actively on, and not started because it is blocked twice: on i-0028 landing in `main`, and on one observed turn through the private route, which is David's to run against his Windows server. The two observations that unblock it are exactly the body's: whether the pinned adapter calls `/v1/messages/count_tokens` and whether `llama-server` answers it, read from the server log and the proxy's app log; and what a gpt-oss-20b thinking block looks like in the transcript. Once both are on this issue as a comment, it is a light-model start. A caution for whoever takes it: the body's "before either" step writes a state file by hand; if i-0028's picker row and setting exist by then, use those, since the hand-written file is a debugging path and not the documented one.
