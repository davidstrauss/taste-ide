---
author: primary
created: 2026-09-16T02:28:59Z
---

Triage (coordinator, 2026-09-16): kept, correctly split from i-0036 under that issue's own allowance, and placed directly under it. Not started: it depends on i-0036 landing, since the two must agree on what "the project's" means for a key, and i-0036 is live now. The body's central finding is right and is the reason this is not a rename: the per-environment key exists to stop N agents writing one home, so the honest shape is a split into a shared per-workspace-and-agent identity volume beside a per-environment conversation home, not a re-key of the volume that exists. Two things for whoever takes it. First, the CLAUDE.md rule that the cwd and the home volume are identical in both topologies is the constraint that makes this a four-spawn-site change or none; list the four in the first comment before editing any. Second, the mid-turn re-login case is the one that needs a decided answer rather than a mutex: say what the second environment sees when the first rotates the identity under it. Model when started: opus[1m], since the concurrency question is a design one.
