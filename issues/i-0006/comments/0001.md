---
author: primary
created: 2026-09-08T16:23:52Z
---

Declined as already fixed on main — the behavior this asked for is there, but not from this branch, so this is "the work on `agents/i-0006` will not land", not "this was done here".

`1e4ec57` ("The coordinator's deadline measures silence, so a conversation stops being restarted out from under itself", 2026-09-08 07:52) is the fix in full: `Streaming` stops being a restart reason on its own, "was the errand answered?" moves out of the `Idle` arm, anything within the deadline re-arms on the same restart count, and `verdict` finally consults `ChatFacts::idle_for_secs` — which was already there and already correct.

This environment's clone is based at `63c2a4a`, six commits behind main and from before that fix landed, so its agent read a tree where the bug was still present and re-derived the same repair as `dbd95bb` (`coordinator.rs` ~217 lines against `1e4ec57`'s ~218, plus `docs/ENVIRONMENTS.md` in both). Merging it would mean resolving a conflict inside the same function against the shipped fix, for no behavioral gain.

Closing this on the `completed` path was refused, correctly, because `agents/i-0006` is 1 ahead of main and the gate does not take anyone's word for a merge. `agents/i-0006` stays on the ref, so `dbd95bb` is readable if the two solutions are ever worth comparing.

The stale base is a separate defect and is not recorded anywhere yet: this environment started at 14:18, six hours after `1e4ec57` landed, and still cloned from before it. An environment that starts behind main will keep producing duplicate work like this one.
