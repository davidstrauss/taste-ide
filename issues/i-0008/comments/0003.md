---
author: primary
created: 2026-09-10T07:25:24Z
---

Reviewed and merged into main as 6de099b (`--no-ff` over 50f9e3b).

Gate, re-run by the coordinator on the merged result: `cargo fmt --all -- --check` clean, `cargo clippy --workspace --all-targets -- -D warnings` exit 0, `cargo test --workspace` exit 0 across every binary, and `git merge-tree` reported no conflict before the merge was taken.

Probe: `build-aux/headless/shoot.sh orchestrator` under `TASTE_PROBE_CHAT=busy`, looked at. Dispatch renders whole — its chips, its half-written line, and its three send buttons — and the permission card's Deny and Allow sit as buttons, which is the design this issue argues for: no keyboard route to stop a turn or deny a card. Running the binary bare under `TASTE_PROBE_CHECK=1` instead of through shoot.sh blocks on a parentless GtkDialog and never reaches its screenshot stage; that is a probe wart, not a defect in this change, and it cost this review an hour.

On the ordering worry in the body — "land i-0012 first, then remove item 4": i-0012 was never started, and this branch did i-0012's work itself, in the same commit, to i-0012's spec, including extracting the decision into `enter_sends(shift, preedit)` so a test can call it. `ChatPane.entry` is a clone of `composer.entry` (chat.rs:1333), so removing item 4 left no orphaned box, and no `escape()` caller survives. The feared regression therefore does not exist at any commit boundary. i-0012 has been declined as delivered here.
