---
author: i-0017
created: 2026-09-10T07:16:26Z
---

Fixed in e11cf89 (branch agents/i-0017).

- Widened the command-block gate rather than duplicating it: `is_command_call(kind, title)` accepts `ToolKind::Execute` (the adapter case, unchanged) or a title that `mcp_tool_name` resolves to `ide_exec`/`ide_exec_output` (the IDE's own command tools, whatever kind the adapter typed the call with) — mirrors `act_kind`'s existing precedent for `issue_create` and friends. Left a comment at the gate explaining why `ToolKind` alone is not enough, so it does not get "simplified" back.
- `IN` is sourced from the call's own input, never its result: added a `cmd_input` field to `ToolCard` (populated for every call, not just acts), and `command_input_line` builds `IN` from `command`/`args` for `ide_exec` and from the handle for `ide_exec_output`. Confirmed by test (`ide_execs_in_line_is_the_call_not_the_wrapper`) that this never touches the result's wrapped `podman exec … sh -c …` string — i-0016 stays untouched by this.
- Settled the two secondary asymmetries with comments, not behavior changes: the opener stays unconditional (a command's point is often just the exit code or last line, already on the digest), and `Read`/`Search`/`Fetch` stay untagged because their titles already state the ask.
- Added the fixture the issue asked for (an `ide_exec` call with `ToolKind::Other`, the shape it actually arrives in) to the probe transcript, plus two unit tests (`ide_exec_is_a_command_even_with_no_useful_kind`, `ide_execs_in_line_is_the_call_not_the_wrapper`).
- Gate: fmt clean, clippy clean, `cargo test --workspace` green except one unrelated, pre-existing `taste-core` watcher test failure from host inotify-instance exhaustion under concurrent workspace tests — confirmed by re-running that test alone, which passes. Probed `TASTE_PROBE_VIEW=watching TASTE_PROBE_CHAT=busy` (orchestrator's shade covers the transcript, so used the view that actually shows it): the new fixture renders a proper IN/OUT block (`cargo test -p taste-app chat` / `test result: ok. 214 passed; 0 failed; 0 ignored`), distinct from the untagged Read/Search rows beside it. No new GTK warnings, no near-miss misalignments, chat's minimum width unchanged at 320px.
