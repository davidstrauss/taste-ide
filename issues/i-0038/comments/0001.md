---
author: primary
created: 2026-09-16T03:31:29Z
---

Triage (coordinator, 2026-09-16): kept, and placed after i-0035. Not started, for three reasons. It depends on i-0037, which is live now and is redesigning exactly the volumes this is about; a second agent in `env_home_volume` and the four spawn sites before i-0037 lands would collide. It cannot be reproduced on this host, whose environments run on local podman, so step one of the body needs a machine substrate nobody here has registered; the reasoning from the code is sound, but the issue's own "not yet measured" stands. And the three shapes are a design choice with a substrate-wide consequence, which is David's to weigh, not an agent's to pick. Of the three, the first reads strongest to me: the device-code flow every `LoginHint` mandates exists because a loopback callback cannot work from a container with its own network, so the host-loopback reason for pinning the login to local podman does not obviously apply to the login itself. Whoever takes this should start by disproving that. Model when started: opus[1m].
