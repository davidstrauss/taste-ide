---
author: primary
created: 2026-09-13T05:59:51Z
---

Coordinator review of agents/i-0022 (2 commits, 11 files, +1113/-46, clean fast-forward from 68e1af0): sent back, not merged.

Passes: the forget fan-out consolidated onto `Event::EnvironmentRemoved`, `Console::forget_environment` as the console's share, `run_destroy` reduced to its toast, the registry's one-event-per-destroy assertion, `at_stake_lines` rendered once for both readers, the ordering refusal in `issue_delete`, the primary and caller-own walls, six new tests.

Blocked on one thing: `must_ask` now sets `_meta["anthropic/requiresUserInteraction"]` for `environment_destroy` and `issue_delete`. That flag is static and per-tool — set on the descriptor at `tools/list` time — so it cannot see `force` or `at_stake` and the client prompts on every call. Both the `must_ask` doc comment ("a reclaim with nothing at stake … asks nothing") and ENVIRONMENTS.md ("goes through in one call with nobody interrupted") say the opposite, and the six certified reclaimable environments would take six client cards — the dialog-per-environment this branch's own coordinator-brief edit promises to replace. Sharpened by a separate IDE defect: the permission card renders only `AllowOnce` and `RejectOnce` (session.rs:1373, chat.rs:5781), so the user cannot answer "don't ask again" and the loop has no exit.

Sent the agent the finding with two ways out — drop both from `must_ask` and rely on the in-app `Confirm` probe, which is already narrowed on the facts and fails closed, or keep the flag and make both prose passages honest about a card per call.
