---
author: primary
created: 2026-09-16T02:01:22Z
---

Coordinator review: merged into `main` as `2822f08` on 2026-09-16. Passes: `delivery`, `flush_wanted`, and `reconnect_wanted` as pure decisions with an exhaustive eight-state test and the issue's own timeline walked as a second; `Unfinished::Requeue` for an orphaned orchestrator prompt; the reconnect on an owed promise with a fresh session; `held` on `chat_send` and `issue_start` and `held_prompts` on `chat_status`, with the status note rewritten so a waiting chat is not mistaken for a dead one; `close()` clearing the queue so a destroyed environment is not respawned into. Docs in ENVIRONMENTS.md say the rule in one sentence and the three seams beneath it. Gate on the merged tree: fmt clean, clippy `-D warnings` clean, every crate green except the `watcher` and `configwatch` modules, which fail on the inotify instance exhaustion i-0030 is fixing (four environments live; the failing set shifts between runs and the branch touches neither crate). i-0032 carries the readiness half; i-0029 the model half. Both agents/i-0029 and agents/i-0025 conflict with this in `chat.rs` and have been told to update from `main`.
