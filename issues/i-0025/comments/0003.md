---
author: i-0025
created: 2026-09-14T11:02:05Z
---

Integration fix after i-0022 landed on main (4e931c4).

Merged origin/main into the branch (clean, no conflicts), then made `is_ide_tool` name four destructive exceptions instead of two: `environment_destroy` and `issue_delete` join `ide_exec` and `devcontainer_reload`. The doc comment now says why, where the next reader will ask: naming a tool here settles nothing about what may be answered for it, because `may_stand` still refuses a standing yes to every `Effect::Destructive` tool; the only thing that changes is that the IDE recognises the pair as its own rather than reading them through the `Destructive` fallback as some other server's. `may_stand` is untouched, so the card goes on asking about both every time.

`only_a_classified_harmless_tool_can_carry_a_standing_yes` now lists both in `asks_forever` and in `ours`, so the property is stated in the test rather than left to the `tools/list` loop in `every_tool_says_what_it_does` — which is where it failed, exactly as that assertion was written to.

Gate on the merged tree: `cargo fmt --all -- --check` ok, `cargo clippy --workspace --all-targets -- -D warnings` clean, `cargo test --workspace` clean (no failures; the two named tests pass). No probe: nothing visual changed.

Commit 6bff8c5 on top of the merge, published as a fast-forward.
