---
author: primary
created: 2026-09-08T16:46:37Z
---

Reviewed and merged as `d5bb625` (pre-merge main was `65839c3`). `agents/i-0004` → `8ed1a5a`, "A finished turn stops interrupting a window the user is already at".

**The change.** `Moment::TurnEnded` now consults `attention.window_active` alone instead of `looking_at(attention.chat_on_screen)`. Every other moment keeps the focus-AND-surface rule, which is the part that matters: a permission prompt in a background tab must still speak, and it does. `chat.rs` gets a three-line comment and no behavior; `docs/ARCHITECTURE.md` and `docs/ENVIRONMENTS.md` both state the exception where they state the rule.

**The other half of the title needed nothing.** "Or enters the foreground" was already handled — `TurnEnded` carries `informational: true`, and `window.rs:2850` withdraws informational notices on `connect_is_active_notify`. The agent checked rather than inventing work there, which was the right answer.

**Tests.** Better than the brief asked for. `a_finished_turn_is_silent_whenever_the_window_has_focus` sweeps all four combinations of `chat_on_screen` × `fleet_on_screen` with the window focused, asserts silence in every one, and then re-runs each with `window_active: false` to prove it is focus doing the suppressing rather than the moment having gone mute. It closes by asserting every other moment kept its surface conjunct. The two existing tests were updated rather than deleted, and the decision-table test now states the exception in its own prose.

**Gate, re-run here on the merge result** — the branch was verified against a base six commits behind main, so its green was not evidence about this tree. `cargo fmt --all -- --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean. `cargo test -p taste-app --bin taste-ide notify::` — 10 passed, 0 failed, including the new test.

**One thing unresolved, and not this change's.** `cargo test --workspace` in the primary environment's container exits 101 on a SIGSEGV in the `taste-app` test binary, inside the `chat::` GTK tests. It is nondeterministic — a different test each run (`chat::tests::escape_stops_only_while_streaming`, then `chat::tests::a_finished_thought_reports_how_long_it_took`) — no test asserts false, and nothing this branch touched is on that path. The i-0004 environment's own freshly rebuilt container ran the full workspace clean (32 binaries, 0 failures), so the crash tracks the long-lived primary container rather than the tree. Inferred, not proven: proving it would mean a cold build of a pre-merge worktree, which the memory rules make expensive. Worth its own issue.
