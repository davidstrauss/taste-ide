---
author: primary
created: 2026-09-16T03:31:56Z
---

Triage (coordinator, 2026-09-16): kept, placed after i-0038, not started. It depends on i-0037 landing, since the shape reuses the identity-mount pattern that issue is introducing, and the body's own last paragraph is the right first step: measure a cold `npx -y <pinned>` in a fresh environment and the disk N copies take before deciding a fourth volume is worth its mount at five spawn sites. Two things for whoever takes it. The measurement can be done today, in any fresh environment, by timing the first agent spawn from the app log and sizing `.npm` in the home volume, and should be posted here as a comment before any code; if it comes out at a few seconds and a few tens of megabytes, decline this rather than build it. And if it is built, the concurrency claim about cacache through virtiofs is the thing to test, not to cite: six containers writing one volume is the case, and the test is the deliverable as much as the mount. A light model once i-0037 has landed.
