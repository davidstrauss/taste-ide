---
author: i-0029
created: 2026-09-16T01:58:53Z
---

i-0029 environment, 2026-09-16: **done and published on `agents/i-0029`** (two commits, rebased onto `7e7f1d3`).

Against the issue's three bullets:

1. *A model chosen at `issue_start` survives a failed first spawn, or the start is refused with the agent's list.* It always survived — that is the finding, and there is now a test that says so. What it did not survive is a session that does not advertise it. Where a session already exists the refusal is returned to the caller as before; where the agent is held for its container — which is every `issue_start` — the reply now names the choice as `model_pending` rather than `model`, says in its note that nothing has confirmed it, and points at `chat_status`. Silence is gone from all three surfaces.
2. *`chat_status` reports the model the session is actually running, kept apart from a pending choice.* It now carries four fields instead of one nullable `model`: `model` (what the session reports it is running, the agent's own default included), `model_pending`, `model_refused`, and `models_advertised`. The refusal also rides in `note`, because a field nobody was looking for is a field nobody checks.
3. *A test in `chats.rs`.* `a_chosen_model_survives_a_first_spawn_that_dies`, plus `a_model_the_agent_does_not_advertise_is_refused_by_name_not_dropped_in_silence` beside it, plus `a_chosen_model_is_in_force_advertised_or_refused_by_name` in `chat.rs` over the real advertised list.

The mechanism lives in one pure `resolve_model` that `build_model_controls` and `create_orchestrated` both consult, so the apply and the report cannot disagree again. **The reconnect path was not touched** — i-0011 had it to itself throughout.

Two things beyond the code: the coordinator's brief (rule 4) now says model ids are exact rather than family names, `opus` is not `opus[1m]`, and that `chat_status` is where the verdict lands — that is where the wrong id gets chosen, and it was chosen twice in one night. And docs/ENVIRONMENTS.md's promise is corrected to the two branches it can actually keep.

**The gate.** `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets -- -D warnings` clean. `cargo test --workspace` passes except for four tests that fail on this host whatever is under test — three in `taste-core`'s `watcher::` and one in `taste-devcontainer`'s `configwatch::` — all of them `Too many open files (os error 24): the per-user inotify instance limit … is exhausted`, which the IDE's own log reports against i-0025 at 01:31:12. Filed as **i-0030** rather than widened into here. Everything else is green: 153 in `taste-app`, 65 in `taste-mcp`, 144 in `taste-core`, 103 in `taste-devcontainer`, and the rest.

Nothing visual changed beyond one subtitle in the model row of the settings shade, which is not in any probe view, so no probe was run.
