---
author: i-0029
created: 2026-09-16T02:06:53Z
---

Merge of `main` (2822f08, which carries i-0011) into this environment's branch is **in progress and stopped one file short**. `MERGE_HEAD` is set and the tree is left as-is so resuming is cheap; `git merge --abort` undoes it if that is preferred.

What git resolved by itself, with no conflict:

- `crates/taste-app/src/chat.rs` — auto-merged. The coordinator expected this one to conflict; it did not. i-0011's `submit_prompt`/`delivery`/`hold_send`/`schedule_reconnect` work and this branch's `resolve_model`/`ModelOutcome`/`model_outcome`/`model_refused` work landed in disjoint regions.
- `crates/taste-core/src/orchestration.rs`, `crates/taste-mcp/src/server.rs`, `crates/taste-app/src/coordinator.rs`, `crates/taste-acp/tests/orchestrator.rs`, `docs/ENVIRONMENTS.md` — auto-merged.

What conflicts, one file, one region:

- `crates/taste-mcp/src/orchestration.rs` — both sides rewrote `chat_facts_json`'s `json!` block and the `note` above it. This branch added `model`, `model_pending`, `model_refused`, and `models_advertised`, and hoisted the note into `state_note`/`note` locals because `json!` will not take a block expression. i-0011 added `held_prompts`. The resolution wants both, and a textual "take ours"/"take theirs" is wrong either way.

Why it is not resolved: `fs/read_text_file` is dead in this session — i-0027, triggered by this environment's own `publish --ready` stopping the container under the live agent at 01:59:01 and the IDE respawning it two seconds later. Details are in a comment on i-0027. The house rule forbids reading the file through `ide_exec`, so the conflict cannot be read, and `Edit`/`Write` refuse a file that was never read. The merge is therefore blocked on i-0027, not on anything about this change.

On the coordinator's design note: this fix does **not** re-apply a pending model on reconnect, so neither `ensure_client(resume)` inside `schedule_reconnect`'s timeout nor the `Ready` arm beside `flush_revive_queue` needs to change. The value was never being lost on reconnect — `model_value` is a plain `RefCell` that `Closed`, `reset_session(false)`, and `ensure_client` all leave alone, and i-0026 proves it (started on `sonnet`, first spawn died, `chat_status` has said `sonnet` ever since). The mechanism was the other candidate: an id the agent does not advertise, with the refusal structurally unable to reach the caller because `issue_start` outside the primary always takes the `awaiting_container()` branch. The fix lives where the value is held, exactly as the brief preferred.

The gate has not been rerun on the merged tree: it cannot compile with conflict markers in it.
