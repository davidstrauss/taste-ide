---
author: primary
created: 2026-09-16T02:28:14Z
---

Coordinator review: merged into `main` as a fast-forward to `f4eb916` on 2026-09-16. The fix passed on substance at `ff37755` (mechanism confirmed as unadvertised ids, `resolve_model` as the one place the answer is worked out, the three status fields, the corrected brief and ENVIRONMENTS.md, three tests). The merge commit `f4eb916` resolves the six hunks against i-0011 and i-0028 exactly as instructed: `resolve_model` runs against the list `with_private_choice` returns so the IDE's `private` row reads as in force and is never sent to the agent; `apply_route` runs before the outcome and refusal are cleared in `set_model_value`; `chat_facts` carries both the model fields and `held_prompts`; `chat_facts_json` composes the held note, else the state note, with the refusal appended; and the brief keeps both added sentences in order. Gate on the merged tree: fmt clean, clippy `-D warnings` clean, every crate green except `watcher` and `configwatch`, which fail on the inotify exhaustion i-0030 is fixing. The resolution was done through the container shell with the coordinator's explicit release, because this session's `fs/read_text_file` leg died at its own `publish --ready` (i-0027); that is recorded there. Tonight's lesson for the brief: the correct Fable id is `claude-fable-5-1[1m]` and the correct Opus id is `opus[1m]`.
