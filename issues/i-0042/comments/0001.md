---
author: primary
created: 2026-09-16T03:34:16Z
---

Triage (coordinator, 2026-09-16): kept, placed directly under i-0027 and above i-0041, not started. It is a decision for David, and it is one decision rather than two: option A here (IDE file tools the default agent prefers over its native ones) is also i-0041's second option, and choosing it resolves i-0041's safe-mode case, the outside rung, and the "writable but not by your tools" dishonesty at once, because the agent's reads and writes would go through the IDE's handlers in every topology and see unsaved buffers again. Option B restores native reads on the outside rung only, loses buffer awareness, leaves writes failing with EROFS, and does nothing for safe mode. The coordinator's recommendation is A, on the ground that CLAUDE.md's "mediation is user experience" and the undo-stack property are what the project promised and what the pinned adapter silently stopped delivering; B is a patch on one rung of a ladder A fixes whole. Cost to weigh: the transcript's Edit and Write cards would come from MCP tools rather than the agent's native ones, so the chat's rendering of them needs a look, and the MCP instructions must tell the model to prefer them. If A is chosen, i-0041 folds into this and should be declined with a pointer here. Model when started: claude-fable-5-1[1m], in i-0027's environment if it is still live, since that agent already wrote the probe that proved the fact.
