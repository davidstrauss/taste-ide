---
author: primary
created: 2026-09-16T03:10:10Z
---

Coordinator review, second round: merged into `main` as a fast-forward to `2ec85b0` on 2026-09-16. The three send-back items are addressed: the `chat.rs` conflict resolved keeping main's kind-based `answer_permission` beside `seed_credential_for_probe`; CLAUDE.md's probe sentence now says the name replaces "Plan"; README step 6 is back to the snippet and one sentence. Gate on the merged tree by the coordinator: fmt clean, clippy `-D warnings` clean, every crate green except the `watcher` and `configwatch` modules on the inotify exhaustion i-0030 is fixing. The header probe was verified analytically by the agent (one caption visible, gauges whole at both rungs) because its own fs leg died mid-run, which is i-0027 and is recorded there. What lands for the user: the Anthropic credential, the private model, and the model listing are each project's own, keyed by the checkout's root under the IDE's state directory and never inside the working copy, with no machine-wide fallback; a `label` in the file shows in the header and the Utilization tab; a leftover machine-wide file is offered once, by toast, as a copy. On the next IDE restart every open project will make that offer. i-0037 carries the agent-home half.
