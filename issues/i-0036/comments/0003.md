---
author: i-0036
created: 2026-09-16T02:57:21Z
---

**Done and republished at `2ec85b0`, merged with `main` at `9ffa8e2`.** The three review items are addressed and the gate is below.

**1. The conflict.** One region in `chat.rs`, resolved as you said: main's `build_permission_answers` and `answer_permission(&self, kind: PermissionOptionKind)` kept, this branch's `seed_credential_for_probe` kept, and the old `answer_permission(&self, allowed: bool)` signature dropped — the body below it reads the clicked option's kind throughout (`kind_allows(kind)`), so the two-state signature does not compile against it. Merge, not rebase.

**2. CLAUDE.md** now says what ships: the name REPLACES "Plan", as "Private" does, because the slot holds one caption.

**3. README step 6** is back to steps — the snippet, one sentence saying the file is per project and that the project must be opened once for the directory's name to exist, and the pre-existing `kind`/`context_tokens` sentence. The reasoning lives in ENVIRONMENTS.md.

**Gate, on the merged tree.** `cargo fmt --all -- --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean. `cargo test --workspace --no-fail-fast`: everything passes except the 5 `watcher` and 7 `configwatch` tests, which fail with `Too many open files (os error 24)` on `fs.inotify.max_user_instances` — i-0030, as expected on this host. Counts: taste-app 159, taste-acp 68 + 5 + 2, taste-authproxy 36 + 20, taste-git 77, taste-mcp 66, taste-core 151 of 156, taste-devcontainer 97 of 104.

**Probes**, both with `TASTE_PROBE_CREDENTIAL=work`. At 1440x900 on `utilization` and at the consolidated rung on `consolidated`, `fit` is `ok` for every pane, and the quota box's geometry says exactly what it should at both: "Plan" hidden at `w=0`, the name visible at its full natural `w=26` (so unellipsized — its `min` is 9, the ellipsis), the private mark hidden at `w=0`, and both gauges whole at 40. `near-miss` on the chat pane reports the same 22/26 pair it did before this branch, plus two rows on i-0025's new permission card, which this branch does not touch; the consolidated dump reports none.

**One honest gap.** I could not *look* at this run's frames: `fs/read_text_file` started answering "File does not exist" for every path during the container reload — i-0027 — so the PNGs are unreadable to me even though the probe wrote them. What I verified analytically is above. The pixels of these same two views at these same two rungs were looked at earlier in this session, before the merge, and read "Claude Code ▰ work ▰" whole at 1440x900 and cut an absurd label to "acme-corp-…" with both gauges whole at 900; the merge since then touched the permission card and the docs, not the header. If you want eyes on it before merging, the two commands are in the gate above and the frames land in `/tmp/probe-{chat,window}.png`.

That same failure is why the conflict resolution and the two edits went through the container shell rather than the IDE's own calls, per your instruction. The write leg failed first, with `EROFS`, while `ide_write_policy` still reported the workspace writable — worth a look alongside i-0027, since it is the same leg failing in a different way after a publish-stop-reload cycle.

The agent-home-volume half remains **i-0037**, filed and linked, for the reasons in the comment above.
