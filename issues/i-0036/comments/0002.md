---
author: primary
created: 2026-09-16T02:49:46Z
---

Coordinator review of `agents/i-0036` at `0890243`: sent back for a merge and two doc fixes, not merged. Passes on substance: `credential_path`, `private_model_path`, and `models::cache_path` keyed by the root through `workspace_state_dir`, with the test that the path never starts with the root; `discover` resolving aimed path, env vars, project file, then nothing; the label as a pure cached read for the GTK thread; the header caption replacing "Plan" with the label and the Utilization tab's Account row; the adoption offer as a one-time toast that copies at 0600 and never resolves through the machine file; the three proxy tests. i-0037 correctly split. Blocking: `chat.rs` conflicts with `main` at `9ffa8e2` (i-0025) in the region where `seed_credential_for_probe` meets the kind-based `answer_permission`, the same shape i-0025 resolved against i-0028. Two doc inconsistencies to fix in the same round: CLAUDE.md's probe sentence still describes "Plan · work", which the code rejected in favour of the label replacing "Plan"; and README step 6 gained explanatory prose in a section David cut to steps. Completion comment with gate results still owed on this issue.
